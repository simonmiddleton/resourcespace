Stores any custom site text the user has entered replacing that stored in the language folder.
page,varchar(50),
name,varchar(200),
text,text,
ref,int(11),
language,varchar(10),
ignore_me,int(11),
specific_to_group,int(11),
custom,int(11),
